<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hvisions.mes.mapper.MatrixReplyMapper">

    <insert id="insertMatrixReply">
        insert into matrix_reply(matrix_id, matrix_name,  task_user, principle)
        values (#{matrixId} ,#{matrixName} ,#{taskUser} ,#{principle} )
    </insert>
</mapper>
